# 딥러닝 프레임워크
tensorflow==2.10.0

# 수치 및 과학 계산 (TensorFlow 2.10 호환 버전)
numpy==1.23.5            # TensorFlow 2.10은 NumPy 1.23.5와 가장 안정적입니다.
scipy==1.10.1

# 오디오 처리
librosa==0.10.1          # 음성 신호 처리의 핵심
imageio-ffmpeg           # M4A 파일 지원 (FFmpeg 자동 설치)

# 데이터 처리
pandas==1.5.3

# 시각화 (결과 확인용)
matplotlib==3.7.1
seaborn==0.12.2

# 기계 학습 (train_test_split 등)
scikit-learn==1.2.2

# 서버 구축용 (나중에 사용)
flask==2.3.2
